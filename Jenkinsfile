pipeline {
         agent any
         stages {
                 stage('test') {
                  steps {
                     sh "echo staring testing the app"
                  }
                 }
                 stage('build') {
                  steps {
                      sh  "echo App is building"
                   }
                 }
             
      }
}
